dependencies:
  pre:
    - pip install ez_setup

test:
  override:
    - ./test:
        environment:
          POOTPOOT_GAE_HOME=~/venv/lib/python2.7/site-packages/google/appengine/

deploy:
  master:
    branch: master
    commands:
      - ./deploy
